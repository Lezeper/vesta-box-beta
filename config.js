const id = '123bb122'; // use for getting config info
const serverIp = '[SERVER_IP]';

module.exports = {
  id: id,
  serverIp: serverIp,

  connectTimeout: 10000,
  dateFormat: 'MM/DD/YYYY, h:mm:ss',
  reportRssiInterval: 1000, // report to server
}